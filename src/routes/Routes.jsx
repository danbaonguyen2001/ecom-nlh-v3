import React from "react";
import { Redirect, Routes, Switch } from "react-router-dom";
import ErrorBoundary from "../components/error/ErrorBoundary";

const Components = () => {
  return (
    <ErrorBoundary>
      {/* Public -Routes */}
      {/* Private- Routes */}
    </ErrorBoundary>
  );
};

export default Components;
